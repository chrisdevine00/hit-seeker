# Run tests and build - these must pass
npm run test:run && npm run build

# Run lint but don't block commit (existing errors need fixing)
# TODO: Remove "|| true" once lint errors are fixed
npm run lint || true
